# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain sensitive data, such as
# password, private keys, and other secrets. Alternatively, you could
# exclude specific .tfvars files if you have some that can be committed.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and should not be committed
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore any .terraform.lock.hcl files, which may contain provider selection information.
# .terraform.lock.hcl

# Ignore any .env files, as they usually contain environment-specific secrets.
.env

# Ignore any files related to local testing or development
local.tf
local.tfvars

# Ignore any files that may contain machine-specific or user-specific data
.terraform-version